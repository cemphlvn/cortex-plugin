---
description: "Initialize cortex project state"
allowed-tools: Bash, Write
---

# Initialize

Edit `.claude/context.md` to define your project ontology.

The plugin is ready. Core files exist:
- `context.md` - project ontology
- `state.jsonl` - history
- `manifest.json` - registry

Run `/start` to bootstrap as meta-agent.
